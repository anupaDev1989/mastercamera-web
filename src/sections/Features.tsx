import React from 'react';
import { Process } from '@/components/ui/process';

const Features = () => {
    return (
        <div id="features">
            <Process />
        </div>
    );
};

export default Features;
